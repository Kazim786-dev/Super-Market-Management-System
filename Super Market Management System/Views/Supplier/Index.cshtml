@model IEnumerable<Supplier>

@{
    ViewData["Title"] = "View Supplier";
}

<div class="container p-4">
    <div class="row p-3">
        <div class="column col-6">
            <h2 class="text-primary">Supplier</h2>
        </div>
        <div class="column col-6 text-end" >
           <a class="btn btn-primary" asp-action="Create" asp-controller="Supplier"><i class="bi bi-plus-circle"></i>&nbsp; New Supplier</a>
        </div>
    </div>

</div>

<table class="table table-hover">
    <thead class="thead-dark table-primary">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Created At</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.supplierid</td>
                <td>@item.name</td>
                <td>@item.email</td>
                <td>@item.phone</td>
                <td>@item.created_at</td>
                <td>
                    <a asp-action="Edit" asp-controller="Supplier" asp-route-id="@item.supplierid"><i class="bi bi-pencil-square"></i></a>
                    &nbsp;&nbsp;
                    <a asp-action="Delete" asp-controller="Supplier" asp-route-id="@item.supplierid"><i class="bi bi-trash3"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
